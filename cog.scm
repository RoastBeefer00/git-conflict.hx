(define package-name 'git-conflict.hx)
(define version "0.1.0")

;; Pure Steel + helix builtins (helix/misc script highlights, steel/process for
;; git). No external dependencies.
(define dependencies '())

(define dylibs '())
